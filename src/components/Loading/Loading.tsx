import { Box } from "@mui/material";

const Loading = () => {
  return (
    <Box sx={{ backgroundColor: "red", color: "white", fontSize: "32px" }}>
      Loading...
    </Box>
  );
};

export default Loading;
